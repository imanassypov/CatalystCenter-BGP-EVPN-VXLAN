# macOS metadata
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# SSH keys / certificates (never commit private keys)
*.pem
**/dcloud-splunk-creds/
**/splunk-creds/

# Local credentials / environment (use the .example templates instead)
.envrc
.vscode/mcp.json

# Splunk app archives (app is tracked as source, not tarball)
*.tar.gz
*.spl

# ...but DO commit the patched receiver SOURCE bundle — it is required to build
# the custom otelcol-yangfix collector and is shipped inside the handoff bundle.
!otel-collector/receiver_yang_26_05_27.tar.gz

# Temp files
/tmp/

# Model Maps (Cisco internal reference docs — not for version control)
Model Maps/

# Production config snapshots (device backups — not for version control)
Prod Config Snapshots/

# CML lab topologies & device configs (lab artifacts — not for version control)
EVPN CML Ottawa/
EVPN CML Ottawa adapted configs/
