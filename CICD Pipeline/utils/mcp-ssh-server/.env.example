# Canonical credentials live in CICD Pipeline/.env (one directory up from here).
#
#   cd "../.."   # CICD Pipeline
#   cp .env.example .env
#   direnv allow
#
# Values load automatically when you cd into CICD Pipeline (.envrc + direnv).
# See CICD Pipeline/.env.example for all variables.
