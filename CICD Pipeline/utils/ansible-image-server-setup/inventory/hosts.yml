---
# Single target: the Ubuntu host that will serve SWIM images over HTTP.
# The IP is variablized — set image_server_ip in
# inventory/group_vars/image_servers/vars.yml (or override with -e).
all:
  children:
    image_servers:
      hosts:
        swim_image_server:
          ansible_host: "{{ image_server_ip }}"
