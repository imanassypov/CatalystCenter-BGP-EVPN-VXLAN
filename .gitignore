.DS_Store
*.pptx
.github/copilot-instructions.md

# Editor / IDE local config (not part of the fabric repo)
.cursor/
.vscode/

# Python virtual environments
**/.venv/
venv/
*.pyc

# LinkedIn blog drafts (local-only, not part of the fabric repo)
Linkedin Blogposts/

# Splunk Lantern article drafts (local-only, not part of the fabric repo)
Lantern Blogposts/
# Ansible runtime artifacts
.ansible/
Campus BGP EVPN Splunk Assurance/packaging/dist/

# Ansible vault password files (secrets — never commit)
.vault_pass
**/.vault_pass

# MCP SSH server local credentials (canonical file: CICD Pipeline/.env via direnv)
CICD Pipeline/.env
CICD Pipeline/utils/mcp-ssh-server/.env

# Splunk SSH keys and local credential stores (never commit)
**/dcloud-splunk-creds/
**/splunk-creds/
*.pem
